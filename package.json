{
  "name": "pxl-lang",
  "version": "1.0.4",
  "description": "A domain-specific language for creating pixel art through code",
  "main": "dist/index.js",
  "bin": {
    "pxl": "dist/cli.js",
    "plx": "dist/cli.js"
  },
  "scripts": {
    "build": "npx tsc",
    "prepublishOnly": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "pixel-art",
    "dsl",
    "generative-art"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/pngjs": "^6.0.5",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "pngjs": "^7.0.0"
  }
}
